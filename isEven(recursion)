function isEven(n) {
  if (n < 0) {
    n *= -1;
    return isEven(n);
  } else if (n === 0) {
    return true; 
  } else if (n === 1) {
    return false;
  } else {
    n = n-2;
    return isEven(n);
    }
}
      
console.log(isEven(-7501))
